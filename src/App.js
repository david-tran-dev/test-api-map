import './App.css';
import {MapContainer, TileLayer, Marker, Popup} from 'react-leaflet'
import Map from './Map';

function App( ) {
  return (
    <div className='App'>
     <Map />
    </div>
  );
}

export default App;
